// This file is generated by AI.

import globals from 'globals';
import js from '@eslint/js';
import tseslint from 'typescript-eslint';
import prettier from 'eslint-config-prettier';

export default tseslint.config(
  // 全局忽略
  {
    ignores: ['**/dist/', '**/node_modules/', '**/pkg/', '**/*.d.ts', 'crates/', 'target/'],
  },
  // 基础 TypeScript 规则（所有 TS/TSX 文件）
  {
    files: ['**/*.ts', '**/*.tsx'],
    extends: [js.configs.recommended, ...tseslint.configs.recommended],
    languageOptions: {
      parser: tseslint.parser,
      parserOptions: {
        project: true,
        tsconfigRootDir: import.meta.dirname,
      },
    },
  },
  // VS Code 扩展（Node 环境）
  {
    files: ['packages/vscode-extension/**/*.ts'],
    languageOptions: {
      globals: {
        ...globals.node,
      },
    },
  },
  // Parse Playground（浏览器 + React）
  {
    files: ['packages/parse-playground/**/*.ts', 'packages/parse-playground/**/*.tsx'],
    languageOptions: {
      globals: {
        ...'globals'.browser,
      },
      parserOptions: {
        ecmaFeatures: {
          jsx: true,
        },
      },
    },
    settings: {
      react: {
        version: 'detect',
      },
    },
  },
  // Prettier 格式化检查
  prettier
);
