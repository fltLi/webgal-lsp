// This file is generated by AI.

// 编辑器字形栏 (glyph margin) 图标的共用形状。
//
// 只有一个形状 —— VS Code 调试器的实心三角箭头。两个地方用它:
// * 实时预览的行指示器 (`editor/gutter-live-preview.ts`);
// * 文本预处理第 3 步"当前正在配说话者的对话段" (`novel/PreprocessBlockEmphasis.ts`)。
//
// 形状写成一个样式标记, 由调用方拼进 `glyphMarginClassName`; 颜色与描边在 styles.css 里
// 按类名给 (深浅色各一套)。这样处只维护一个箭头, 而不是两处各抄一份 SVG。

/** 字形栏图标的公共类名 (CSS 里据此画箭头) */
export const EDITOR_BLOCK_GLYPH_CLASS = 'editor-block-glyph';

/** 拼出 `glyphMarginClassName`: 公共形状 + 调用方的配色类 */
export function blockGlyphClassName(colorClassName: string): string {
  return `${EDITOR_BLOCK_GLYPH_CLASS} ${colorClassName}`;
}
