// This file is generated by AI.

// Tauri 命令封装 (WebView 开发者工具)。

import { invoke } from '@tauri-apps/api/core';

/**
 * 打开当前窗口的 WebView 开发者工具。
 *
 * 后端的浏览器快捷键是被整体关闭的 (见 `src-tauri/src/service/browser.rs`), WebView2 因此
 * 不再自己响应 F12, 调试构建下由前端监听 F12 主动调用本命令。发布构建里开发者工具已被
 * 后端彻底关闭, 调用不会有任何效果。
 */
export function openDevtools(): Promise<void> {
  return invoke<void>('open_devtools');
}
