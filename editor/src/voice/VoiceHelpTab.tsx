// This file is generated by AI.

// 配音工作流帮助选项卡: 渲染内置的 Markdown 说明文档。

import { useMemo } from 'react';
import { marked } from 'marked';

import { VOICE_GUIDE_MARKDOWN } from './guide';

export function VoiceHelpTab() {
  const html = useMemo(() => marked.parse(VOICE_GUIDE_MARKDOWN, { async: false }) as string, []);
  return (
    <div className="voice-help">
      {/* 内容来自仓库内固定的 Markdown 文案, 不含用户输入 */}
      <div className="voice-help-body" dangerouslySetInnerHTML={{ __html: html }} />
    </div>
  );
}
